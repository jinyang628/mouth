chrome.runtime.onInstalled.addListener(() => {
    console.log('Extension successfully installed!');
    // Perform additional setup or defaults if necessary
});
  
  // You can add more listeners and functions as needed for your extension's functionality.
  